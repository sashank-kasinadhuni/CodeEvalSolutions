import argparse


def Rightmost_occurance():
    parser = argparse.ArgumentParser()
    parser.add_argument("filename")
    args = parser.parse_args()
    with open(args.filename) as f:
        for line in f:
Rightmost_occurance()        	